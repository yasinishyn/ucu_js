var firstName = 'John',
    lastName = 'Smith',
    age = 30,
    isAgent = true,
    weapons = null,
    mission;

console.log(`
  ${firstName} ${lastName}
  is ${age} years old,
  he has ${weapons} weapons,
  and his mission is ${mission}
`);

// In many teams, and according to some industry standard style-guides,
// the above variables definition is considered to be a bad practice.