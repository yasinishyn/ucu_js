new Boolean(true); // true
new Number(500); // Number {500}
new String('John Smith')

// Not Constructructable
new Symbol("meaning of life") // error. Symbol does not have a constructor

// undefined is a property of the global object.
// That is, it is a variable in global scope.
// The initial value of undefined is the primitive value undefined.
undefined 

// The value null is written with a literal: null.
// null is not an identifier for a property of the global object,
// like undefined can be.
null